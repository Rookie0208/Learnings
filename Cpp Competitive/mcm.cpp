#include<bits/stdc++.h>
using namespace std;
const N = 10
int dp[N][N];
int ind[N][N];

for(int i = 0; i<N; i++) {
	for (int j = 0; j < N; ++i)
	{
		if(i == j) {
		dp[i][j] = 0;
		ind[i][j] = 0;
	} else {
		dp[i][j] = 
	}
	}
}

int main() {
	int a1,a2,b1,b2,c1,c2,d1,d2;
	cin >> a1 >> a2;
	cin >> b1 >> b2;
	cin >> c1 >> c2;
	cin >> d1 >> d2;
	// int a[a1][a2], b[b1][b2], c[c1][c2], d[d1][d2];


}